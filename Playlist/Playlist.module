<?php

/*
 *Implementing hook_theme().
 */

function Playlist_theme($existing, $type, $theme, $path) {

	return array(
		'Playlist' => array(
			'variables' => array('items' => array(), 'title' => '', 'url' => '')
		)
	);
}
